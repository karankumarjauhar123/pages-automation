# Swapped Reels

All processed and face-swapped reels will be automatically saved here by the GitHub Actions workflow.

The output filename format will be:
`swapped_<original_video_name>_with_<influencer_face_name>_<timestamp>.mp4`

You can download them directly from the GitHub web interface or run a `git pull` locally.
